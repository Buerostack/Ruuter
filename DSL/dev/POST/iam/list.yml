list_users:
  call: http.post
  args:
    url: "http://localhost:8082/dev/iam/account/list"
  result: users_list
  next: respond

respond:
  wrapper: false
  return: ${users_list.response.body}
  next: end